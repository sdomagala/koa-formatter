// @flow

type koaFormatter$Options = {
  formatter?: (ctx: koa$Context$Impl, err: any) => any;
};

export type Options = koaFormatter$Options;

declare export default (options?: Options) => (ctx: koa$Context$Impl, next: Function) => Promise<*>;